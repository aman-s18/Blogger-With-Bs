
<%= form_with(model: [ @blog, @blog.comments.build ], local: true) do |form| %>
		  
		  <p>
		    <%= form.label :Comment %><br>
		    <%= form.text_area :body %>
		  </p>
		  <p>
		    <%= form.submit %>
		  </p>

		  
<% end %>